# Since the ".env" file is gitignored, you can use the ".env.example" file to
# build a new ".env" file when you clone the repo. Keep this file up-to-date
# when you add new variables to `.env`.

# This file will be committed to version control, so make sure not to have any
# secrets in it. If you are cloning this repo, create a copy of this file named
# ".env" and populate it with your secrets.

# When adding additional environment variables, the schema in "/src/env.js"
# should be updated accordingly.

# Next Auth
# You can generate a new secret on the command line with:
# npx auth secret
# https://next-auth.js.org/configuration/options#secret
AUTH_SECRET="QTD54ABfY4oiz8lvM6lZ+83BdZiouaHENqKWvcN7i+0=" # Added by `npx auth`. Read more: https://cli.authjs.dev

# Drizzle
DATABASE_URL="postgresql://postgres:password@localhost:5432/server"
NODE_ENV="development"

# GitHub OAuth
AUTH_GITHUB_ID=""
AUTH_GITHUB_SECRET=""

# GitHub Token (for fetching repo updates)
GITHUB_TOKEN=""

# AWS Credentials
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_BUCKET_NAME=""
AWS_REGION="us-east-2"

EXO_KEY="xxxxx"
EXO_SECRET="xxxxxx"
EXO_ZONE="ch-gva-2"
EXO_CLUSTER_ID="xxxxxxx"
EXO_POD_REQUEST_CPU="xxxxxxx"
EXO_POD_REQUEST_MEMORY="xxxxxxx"
EXO_POD_LIMIT_MEMORY="xxxxxxx"

BOT_IMAGE_MEET="ghcr.io/meetingbot/bots/meet:main"
BOT_IMAGE_TEAMS="ghcr.io/meetingbot/bots/teams:main"
BOT_IMAGE_ZOOM="ghcr.io/meetingbot/bots/zoom:main"

BOTS_PROVIDER=AWS # AWS or EXOSCALE

API_URL=http://localhost:3000/api
